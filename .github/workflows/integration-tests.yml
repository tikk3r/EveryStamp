# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: integration tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.7", "3.8", "3.9", "3.10", "3.11"]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Check dependency installation
      run: |
        sudo apt-get update
        sudo apt remove -y shim-signed grub-efi-amd64-bin –allow-remove-essential
        sudo apt-get upgrade -y
        sudo apt-get install libboost-python-dev casacore-dev libcfitsio-dev wcslib-dev luminance-hdr
        python -m pip install --upgrade pip setuptools wheel
        python -m pip install git+https://github.com/tikk3r/EveryStamp.git
    - name: Run tests
      run: |
        bash test/run_tests.sh
