[project]
name = "EveryStamps"
version = "0.0.0"
authors = [
    { name = "Frits Sweijen" },
]
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/tikk3r/EveryStamps"
"Bug Tracker" = "https://github.com/tikk3r/EveryStamps/issues"

[build-system]
requires = ["requests"]